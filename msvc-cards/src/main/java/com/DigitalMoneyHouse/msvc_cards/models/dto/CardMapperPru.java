package com.DigitalMoneyHouse.msvc_cards.models.dto;

import org.springframework.stereotype.Component;


@Component
public class CardMapperPru {
/*
//Convierte Card to creditCardDto
public CardDTO toCreditCardDTO (CreditCard card){

    if (card == null) {
return null;
    }
 CardDTO cardDTO = new CardDTO();

    cardDTO.setExpiration(card.getExpiration());
    cardDTO.setNumber((card.getNumber()));
    cardDTO.setHolderName(card.getHolderName());
    cardDTO.setCvc(card.getCvc());
    cardDTO.setUserId(card.getUserId());
    cardDTO.setAccountId(card.getAccountId());
  //  cardDTO.setCardType(card.get);



}


    //Convierte Card to debitCardDto
    public CardDTO toCreditCardDTO (Card card){
        if (card == null) {
            return null;
        }
        CardDTO cardDTO = new CardDTO();

    }
    //Convierte Card to CardDto
    public CardDTO toCreditCardDTO (Card card){
        if (card == null) {
            return null;
        }
        CardDTO cardDTO = new CardDTO();

    }

*/
}
