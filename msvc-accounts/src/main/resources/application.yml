server:
  port: 8083
spring:
  application:
    name: msvc-accounts

  config:
    import: optional:configserver:http://localhost:8888

  profiles:
    active: local



eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

